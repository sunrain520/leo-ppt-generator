{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://spec-first.dev/schemas/spec-app-consistency-audit/analytics-contract.schema.json",
  "title": "spec-app-consistency-audit analytics contract artifact",
  "type": "object",
  "required": ["schema_version", "artifact_id", "source_inputs", "consumers", "contract_status", "data_sensitivity", "events", "key_path_coverage"],
  "properties": {
    "schema_version": { "const": "analytics-contract.v1" },
    "artifact_id": { "const": "analytics-contract" },
    "source_inputs": { "type": "array", "minItems": 1 },
    "consumers": { "type": "array", "minItems": 1 },
    "contract_status": { "const": "candidate" },
    "data_sensitivity": { "enum": ["public", "internal", "confidential", "restricted"] },
    "events": { "type": "array" },
    "key_path_coverage": { "type": "object" },
    "degraded_modes": { "type": "array" }
  },
  "additionalProperties": true
}
