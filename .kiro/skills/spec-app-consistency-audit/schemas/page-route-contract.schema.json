{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://spec-first.dev/schemas/spec-app-consistency-audit/page-route-contract.schema.json",
  "title": "spec-app-consistency-audit page route contract artifact",
  "type": "object",
  "required": ["schema_version", "artifact_id", "source_inputs", "consumers", "contract_status", "data_sensitivity", "routes", "coverage_gaps"],
  "properties": {
    "schema_version": { "const": "page-route-contract.v1" },
    "artifact_id": { "const": "page-route-contract" },
    "source_inputs": { "type": "array", "minItems": 1 },
    "consumers": { "type": "array", "minItems": 1 },
    "contract_status": { "const": "candidate" },
    "data_sensitivity": { "enum": ["public", "internal", "confidential", "restricted"] },
    "routes": { "type": "array" },
    "coverage_gaps": { "type": "array" },
    "degraded_modes": { "type": "array" }
  },
  "additionalProperties": true
}
